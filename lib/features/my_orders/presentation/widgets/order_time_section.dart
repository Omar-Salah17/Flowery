import 'package:flutter/cupertino.dart';

class OrderTimeSection extends StatelessWidget {
  const OrderTimeSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}